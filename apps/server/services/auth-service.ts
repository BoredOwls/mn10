import { asyncHandler } from "../common/async-handler";
import { ApiResponse } from "../common/api-response";

const regsiter = async (params: any) => {
  // return the business logic
};

export const authService = { regsiter };
