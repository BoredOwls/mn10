


## Setup & usage guide:



1. Install deps
```bash
cd apps/server && bun install
cd apps/web && npm install
```

2. Run with turbo (dev mode)
```bash
npm run dev
```

3. Run containers (optional)
```
npm run up
```
